/* 표현식이란 결과적으로 어떤 값으로 평가(evaluation)되는 것이다. 여기에 표현식을 평가한다는 말은 표현식의 값과 변수, 함수 등의 값을 바탕으로 식의 값을 계산하는 행위를 뜻한다. 가장 간단한 표현식은 숫자, 문자열, 논리값 등의 원시 값(Prinitive value)다. */
3.14, "hello", true, false, null;
/* 변수, 프로퍼티, 배열 요소, 함수 호출, 메서드 호출도 표현식이다. */
SVGNumber, circle.radius, a[3], square(5), car.getSum();
