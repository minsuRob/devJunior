<?php

$li = ['a', 'b', 'c', 'd', 'e'];

array_push ($li,'f'); //배열의 맨 뒤에 'f'라는 문자 추가

array_unshift ($li,'z'); //배열의 맨 앞에 'z'라는 문자 추가

array_shift ($li); //배열의 맨 앞에 문자 제거

array_pop ($li); //배열의 맨 뒤에 문자 제거

?>